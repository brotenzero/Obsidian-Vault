

String
	ㄴ기계입장 forein language
	
int
float
char
boolean
	ㄴ내장 mother language


data 와 value 차이 !!
무결성의 정보만 data( 데이타 베이스에 있는것만 )

객체 : 속성과 기능의 집합

대문자로 시작하는
System
String

subject  기계
object 인간
객체지향
>> 인간지향

실존은 디스크
가상은 메모리
Scanner scanner = new Scanner(System.in)
실존      가상      
타입
클래스  인스턴스
인스턴스는 객체의 이름

인스턴스 코드 끝에서는 gabage 가 됨





디스크> 메모리 :로드
메모리> 디스크 :세이브

자바 타입 두가지
primitive type  
reference type  (프리미티브를 참조)


알고리즘은 명령의 집합

> **명령(Instruction)**이란  
> **컴퓨터가 수행할 수 있는 ‘가장 기본적인 동작 단위’**를 말합니다.

즉,  
알고리즘이 “문제 해결 절차”라면,  
명령은 그 절차를 이루는 **한 단계의 구체적인 지시**입니다.


instruction 기계어 명령어  
statement 우리가 작성 하는 프로그래밍 언어
statement >> 빌드>> instruction

https://docs.oracle.com/javase/tutorial/getStarted/intro/definition.html
자바튜토리얼

코드는 블록으로 구분됨
메소드는 expression 과 statement 의 집합이다
블록으로 구분된다

코드가 메모리로 이동하면 인스트럭션 이 된다
\\

Expression 식
값을 계산해서 하나의 결과를 만드는 식

Statement 문


블록은 expression 과 statement 의 집합

expression 은 ; 이 없음

a = 10   expression
a = 10;  statement

CPU의 **클록 펄스(Clock Pulse)**란, CPU를 비롯한 컴퓨터 내 모든 디지털 회로의 **움직임을 통제하고 시간을 맞추는** 역할을 하는 규칙적인 **전기 신호**를 말합니다.

아주 쉽게 말해, CPU에게 "자, 이제 다음 동작 시작!" 하고 외치는 **메트로놈**이나 **지휘자의 박자**와 같아요.

### 1. 클록 펄스의 역할

- **동기화 (Synchronization):** 클록 펄스는 시스템 내의 모든 구성 요소(CPU, 메모리, 주변 장치 등)가 같은 **일정한 박자**에 맞춰 움직이도록 **타이밍 기준**을 제공합니다. 🥁
    
- **작업 단위 정의:** CPU는 펄스가 한 번 "틱" 할 때마다(한 클록 주기마다) 명령어의 한 단계(가져오기, 해석, 실행 등)를 수행합니다. 💻
-

if(....){....} -> if절
else if (...){.....} -> else if 절
else{....} -> else 절

if 문(statement)

VS Code에서 Java 코드를 실행할 때 한글이 깨지는 가장 흔한 이유는 **인코딩 설정 불일치** 때문입니다

쉽게 말해, 파일을 저장할 때 사용한 '언어 코드표'와, VS Code나 Java 프로그램이 그 파일을 읽을 때 사용하는 '언어 코드표'가 서로 달라서 발생하는 문제입니다


조건식(프롬프트 키워드)


LLM large language model
sLM small language model

String message = "";
>> 주소가 없는것
>
>
null




